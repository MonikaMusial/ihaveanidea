

<p>
  <strong>Great! You are trying this idea</strong>
</p>
<p>
	Title: <%= @idea.title %>
</p>

<p>
	If you already tried, give your rating:
</p>

<%= form_with(model: @user_idea, local: true) do |form| %>

  <div class="field">
  	<% @useridea = UserIdea.where(idea_id:params[:id],user_id:current_user).take %>
    <%= if !@useridea.nil?
           form.text_field :rating, value: @useridea.rating 
        else
           form.text_field :rating
        end
    %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<%= link_to 'Back', ideas_path %>
